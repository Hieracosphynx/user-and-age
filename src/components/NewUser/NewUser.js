import UserForm from './UserForm';
import Card from '../UI/Card';

const NewUser = (props) => {
  return (
    <Card>
      <div>
        <UserForm />
      </div>
    </Card>
  );
};

export default NewUser;
